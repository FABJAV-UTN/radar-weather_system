services:
  api:
    command: uv run fastapi dev app/main.py --host 0.0.0.0 --port 8000
    volumes:
      - ./app:/app/app
      - ./tests:/app/tests
    environment:
      - LOG_LEVEL=debug
      - PYTHONASYNCIODEBUG=1